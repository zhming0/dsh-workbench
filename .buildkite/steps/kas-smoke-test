#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/lib/mise.bash"
ensure_tools

echo "--- :docker: Building the runner and host images"
docker buildx bake dev host-dev --load

echo "+++ :kubernetes: Running the agent-sandbox end-to-end test"
pnpm test:kas
