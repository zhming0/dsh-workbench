apiVersion: v1
kind: ConfigMap
metadata:
  name: dsh-runner-public-key
  namespace: dsh-sandbox
data:
  # Replace this value before admitting untrusted sessions.
  public-key.pem: |
    -----BEGIN PUBLIC KEY-----
    REPLACE_WITH_DSH_PROVIDER_PUBLIC_KEY
    -----END PUBLIC KEY-----
